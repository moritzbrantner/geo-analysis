# geo-core-app

React, TypeScript, TailwindCSS, and Bun frontend for `moenarch-geo-core`.

Run the server:

```bash
cargo run -p moenarch-geo-core-server -- --addr 127.0.0.1:3000
```

Run the app:

```bash
bun --filter @moenarch/geo-core-app dev
```
